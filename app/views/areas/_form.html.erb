<%= simple_form_for (@area) do |f| %>
  <%= f.input :description %>

  <%= f.input :lat %>

  <%= f.input :long %>
  
  <%= f.input :basin_id, collection: @basins %>
  <%= f.button :submit %>
<% end %>
