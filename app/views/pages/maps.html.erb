<h1>Mapa de Todas as Áreas</h1>

<p><%= link_to "Voltar para Áreas Disponíveis", areas_path, class: "btn btn-success button-light-shadow" %>

<div id="map"
      style="width: 100%; height: 800px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
